---
import AuthLayout from "@/layouts/AuthLayout.astro";
import { ResetPasswordForm } from "@/components/auth/ResetPasswordForm";

// Note: Token validation will be handled by the callback page
// which will exchange the token for a session before redirecting here.
// The actual password update uses the authenticated session.
---

<AuthLayout title="Resetowanie hasła">
  <ResetPasswordForm client:load />
</AuthLayout>
